import{useEffect,useState}from'react';import{Link}from'react-router-dom';import{apiFetch}from'../lib/api';type C={email:string;name:string;phone:string;orders:number;total:number;lastOrder:string};export function AdminCustomers(){const[c,setC]=useState<C[]>([]);const[q,setQ]=useState('');const[page,setPage]=useState(1);const[pages,setPages]=useState(1);useEffect(()=>{const t=setTimeout(async()=>{const r=await apiFetch(`/api/admin/customers?${new URLSearchParams({page:String(page),search:q})}`);const d=await r.json();setC(d.customers||[]);setPages(d.pagination?.pages||1)},250);return()=>clearTimeout(t)},[q,page]);return <section className="p-5 lg:p-8"><p className="text-sm font-black uppercase tracking-[.25em] text-[#19A2B6]">CRM</p><h1 className="mt-2 text-4xl font-black">Clientes</h1><input value={q} onChange={e=>{setQ(e.target.value);setPage(1)}} placeholder="Buscar cliente, correo o teléfono" className="mt-7 w-full rounded-2xl border-0 bg-white px-5 py-4 font-bold outline-none"/><div className="mt-5 overflow-x-auto rounded-[1.5rem] bg-white"><table className="w-full min-w-[700px] text-left"><thead><tr className="border-b text-xs uppercase text-black/40">{['Cliente','Pedidos','Comprado','Última compra',''].map(x=><th key={x} className="p-5">{x}</th>)}</tr></thead><tbody>{c.map(x=><tr key={x.email} className="border-b last:border-0"><td className="p-5"><p className="font-black">{x.name}</p><p className="text-xs font-bold text-black/40">{x.email} · {x.phone}</p></td><td className="p-5 font-black">{x.orders}</td><td className="p-5 font-black">${x.total.toLocaleString('es-MX')}</td><td className="p-5 text-sm font-bold text-black/45">{new Date(x.lastOrder).toLocaleDateString('es-MX')}</td><td className="p-5"><Link className="font-black text-[#19A2B6]" to={`/admin/customers/${encodeURIComponent(x.email)}`}>Ver</Link></td></tr>)}</tbody></table></div><div className="mt-5 flex justify-end gap-3"><button disabled={page<=1} onClick={()=>setPage(p=>p-1)} className="rounded-full bg-white px-5 py-2 font-black disabled:opacity-30">Anterior</button><span className="py-2 font-black">{page}/{pages}</span><button disabled={page>=pages} onClick={()=>setPage(p=>p+1)} className="rounded-full bg-white px-5 py-2 font-black disabled:opacity-30">Siguiente</button></div></section>}
