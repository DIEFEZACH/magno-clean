import type { CatalogFamily, CatalogVariant } from "../../types/catalog";

export function VariantSelector({family,selected,onSelect}:{family:CatalogFamily;selected:CatalogVariant;onSelect:(variant:CatalogVariant)=>void}){
  return <fieldset className="mt-7"><legend className="text-sm font-black">{family.variantType}</legend><div className="mt-3 flex flex-wrap gap-2">{family.variants.map((variant)=>{const active=variant.id===selected.id;return <button type="button" key={variant.id} disabled={!variant.available} aria-pressed={active} onClick={()=>onSelect(variant)} className="min-h-11 rounded-full border-2 border-black/10 px-5 py-2 text-sm font-black transition hover:border-[#19A2B6] aria-pressed:border-[#19A2B6] aria-pressed:bg-[#19A2B6] aria-pressed:text-white disabled:cursor-not-allowed disabled:border-black/5 disabled:bg-black/5 disabled:text-black/35 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[#EF8329]"><span>{variant.label}</span>{!variant.available&&<span className="ml-2 text-xs">Agotado</span>}<span className="sr-only">{active?", seleccionado":""}</span></button>})}</div><p className="sr-only" aria-live="polite">Variante seleccionada: {selected.label}, código {selected.code}</p></fieldset>;
}
