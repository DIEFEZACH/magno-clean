import type { CatalogFamily, CatalogVariant } from "../../types/catalog";

export function VariantSelector({family,selected,onSelect}:{family:CatalogFamily;selected:CatalogVariant;onSelect:(variant:CatalogVariant)=>void}){
  return <fieldset className="mt-6"><legend className="text-xs font-bold text-[#142f33]">Elige {family.variantType.toLowerCase()}</legend><div className="mt-3 flex flex-wrap gap-2">{family.variants.map((variant)=>{const active=variant.id===selected.id;return <button type="button" key={variant.id} aria-pressed={active} onClick={()=>onSelect(variant)} className="min-h-12 rounded-lg border border-[#dce4e0] bg-white px-4 py-2.5 text-left text-xs font-bold text-[#355353] transition hover:border-[#168fa1] aria-pressed:border-[#168fa1] aria-pressed:bg-[#e8f3ef] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[#EF8329]"><span>{active&&<span aria-hidden="true" className="mr-1.5 text-[#16818f]">✓</span>}{variant.label}</span>{!variant.available&&<span className="mt-1 block text-[9px] font-medium text-[#8b5650]">Agotado</span>}<span className="sr-only">{active?", seleccionado":""}</span></button>})}</div><p className="mt-3 text-[11px] leading-5 text-black/50">Puedes consultar todas las presentaciones. La compra sólo está disponible cuando hay existencias y las ventas están habilitadas.</p><p className="sr-only" aria-live="polite">Variante seleccionada: {selected.label}, código {selected.code}</p></fieldset>;
}
