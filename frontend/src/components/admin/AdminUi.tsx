/* eslint-disable react-refresh/only-export-components */
export function AdminPageHeader({ eyebrow, title, actions }: { eyebrow: string; title: string; actions?: React.ReactNode }) { return <div className="flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><p className="text-sm font-black uppercase tracking-[.25em] text-[#19A2B6]">{eyebrow}</p><h1 className="mt-2 text-4xl font-black tracking-[-.04em]">{title}</h1></div>{actions}</div>; }
export function Pagination({ page, pages, onPage }: { page: number; pages: number; onPage: (page: number) => void }) { return <nav aria-label="Paginación" className="mt-5 flex items-center justify-end gap-3"><button type="button" disabled={page <= 1} onClick={() => onPage(page - 1)} className="min-h-11 rounded-full bg-white px-5 py-2 font-black disabled:opacity-30">Anterior</button><span className="text-sm font-black">{page} / {pages}</span><button type="button" disabled={page >= pages} onClick={() => onPage(page + 1)} className="min-h-11 rounded-full bg-white px-5 py-2 font-black disabled:opacity-30">Siguiente</button></nav>; }
export function AdminSkeleton() { return <div className="h-48 animate-pulse rounded-[1.5rem] bg-black/[.03]" aria-label="Cargando"/>; }
export const formatCurrency = (value: number, currency = "MXN") => new Intl.NumberFormat("es-MX", { style: "currency", currency }).format(value);
export const formatDate = (value: string) => new Date(value).toLocaleDateString("es-MX");
